#!/usr/bin/python3
"""Module that defines a function to count the keys of a dictionary."""


def number_keys(a_dictionary):
    """Return the number of keys in a dictionary.

    Args:
        a_dictionary (dict): The dictionary to inspect.

    Returns:
        int: The number of keys in a_dictionary.
    """
    return len(a_dictionary)
